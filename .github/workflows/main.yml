name: Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run build
        run: echo "Build done"

      - name: Notify Slack
        uses: ./.github/actions/slack-post
        with:
          webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
          message: "Build finished for ${{ github.repository }} ✅"
